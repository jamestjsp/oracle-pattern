interface:
  display_name: "Oracle Migration"
  short_description: "Capture legacy calls and replay the replacement"
  default_prompt: "Use $oracle-migration to capture a versioned legacy baseline and verify a replacement against its recorded behavior."
